[project]
name = "code-executor"
version = "0.1.0"
description = "MCP Server - Code Executor"
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.0",
    "structlog>=24.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"