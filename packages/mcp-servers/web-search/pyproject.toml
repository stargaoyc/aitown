[project]
name = "web-search"
version = "0.1.0"
description = "MCP Server - Web Search (Tavily API integration)"
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.0",
    "structlog>=24.4",
    "httpx>=0.27",
    "pydantic>=2.10",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
